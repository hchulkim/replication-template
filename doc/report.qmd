---
title: "report"
author: "Hyoungchul Kim"
date: today
format: 
  html:
    toc: true
    number-sections: true
    keep-html: true
    code-overflow: wrap
  pdf:
    pdf-engine: pdflatex
    keep-tex: true
    include-in-header: 
       text: |
         \usepackage{fvextra}
         \DefineVerbatimEnvironment{Highlighting}{Verbatim}{breaklines,commandchars=\\\{\}}
         \DefineVerbatimEnvironment{OutputCode}{Verbatim}{breaklines,commandchars=\\\{\}}
---

# Lorem ipsum

## Bullet point

This is a template for my report.

1. Lorem.

- Lorem.

## Math equations

This is a latex math-equation using *align*.

\begin{align*}
  (1) + (2) + (3) \geq 3
\end{align*}

--

## Code block

This is a code block.

```{r}
#| warning: false
#| message: false

library(fixest)
  mods = feols(
  rating ~ complaints + privileges + learning + csw0(raises + critical), data = attitude
)

dict = c("rating"     = "Overall Rating",
         "complaints" = "Handling of Complaints",
         "privileges" = "No Special Priviledges",
         "learning"   = "Opportunity to Learn",
         "raises"     = "Performance-Based Raises",
         "critical"   = "Too Critical")

library(modelsummary) # Make sure you have >=v2.0.0

modelsummary(
  setNames(mods, c("(1)", "(2)")),
  coef_map = dict, stars = TRUE,
  gof_map = NA
  ) |>
  # some optional stylistic tweaks
  tinytable::group_tt(j = list("Dep. variable: Overall Rating" = 2:3)) |>
  tinytable::style_tt(i = 1:2, j = 2:3, background = "pink")
```

```{python}
# You need reticulate R-package for this
print("hello, python world.")
```

```{julia}

# You need JuliaCall R-package for this
println("hello, julia world.")
```
